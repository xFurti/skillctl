{
  "name": "@skillctl/plugin-system",
  "version": "0.2.0",
  "description": "Plugin loader for skillctl (commands, adapters, registries)",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "lint": "tsc --noEmit",
    "test": "echo 'plugin tests via cli fixture' && exit 0",
    "prepack": "pnpm run build"
  },
  "dependencies": {
    "@skillctl/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.6.3"
  },
  "engines": { "node": ">=20.12" }
}