{
  "name": "@skillctl/plugin-system",
  "version": "0.3.1",
  "description": "Experimental plugin loader for skillctl (commands, adapters, registries)",
  "license": "MIT",
  "author": "xFurti & Gabry848",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "lint": "tsc --noEmit",
    "test": "echo 'plugin tests via cli fixture' && exit 0",
    "prepack": "pnpm run build"
  },
  "dependencies": {
    "@skillctl/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.6.3"
  },
  "engines": { "node": ">=22.13" },
  "repository": {
    "type": "git",
    "url": "https://github.com/xFurti/skillctl.git",
    "directory": "packages/plugin-system"
  },
  "keywords": ["plugins", "extensibility", "agent-skills", "cli"]
}