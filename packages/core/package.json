{
  "name": "@skillctl/core",
  "version": "0.2.0",
  "description": "Core types, config, and FS utilities for skillctl",
  "license": "MIT",
  "author": "skillctl contributors",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rimraf dist",
    "lint": "tsc --noEmit",
    "test": "echo \"no tests yet for core\" && exit 0",
    "prepack": "pnpm run build"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22.13"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/skillctl/skillctl.git",
    "directory": "packages/core"
  },
  "keywords": [
    "core",
    "types",
    "config",
    "agent-skills"
  ]
}
